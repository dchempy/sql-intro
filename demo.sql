-- Get the first and last name of every player in the database.

SELECT first_name, last_name FROM players;

-- sqlite3 baseball.sqlite3
-- .schema -> shows tables
-- .mode columns -> adds columns to terminal output
-- .headers on --> adds headers to terminal output
-- .read _____ --> how to run sql code
-- .quit --> how to quit sqlite3
-- ; --> add if you get ...> to break command
-- CTRL L --> hit keys to clear terminal